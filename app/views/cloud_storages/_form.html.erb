<%= form_with(model: cloud_storage, local: true) do |form| %>
  <% if cloud_storage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cloud_storage.errors.count, "error") %> prohibited this cloud_storage from being saved:</h2>

      <ul>
      <% cloud_storage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :cloud_storage_name %>
  </div>

  <div class="field">
    <%= form.label :access_token %>
    <%= form.text_field :access_token, id: :cloud_storage_access_token %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
